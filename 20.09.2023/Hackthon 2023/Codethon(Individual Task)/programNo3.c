// 3. Write a program to Print Prime Numbers between Two numbers.

#include <stdio.h>

int main()
{
    int s, e, f = 0;
    printf("Enter range :");
    scanf("%d%d", &s, &e);
    for (int i = s; i < f; i++)
    {
        /* code */
    }
    

    return 0;
}
