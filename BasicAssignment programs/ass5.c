//5. WAP to calculate area of circle (a cir=pi*r*r).
#include<stdio.h>
int main()
{
    float pi=3.14,r;
    printf("Enter radius of circle :\n");
    scanf("%f",&r);
    printf("Area of Rectangle is %.2f",(pi*r*r));

}